{
  "_preset": "fullstack",
  "dev": "npm run dev",
  "build": "npm run build",
  "test": "npm test",
  "test:e2e": "npm run test:e2e",
  "lint": "npm run lint",
  "typecheck": "npx tsc --noEmit",
  "_typecheck_note": "If tsconfig.json is a solution file (references only, no top-level `include`), `tsc --noEmit` silently checks NOTHING. Point it at each real project instead, e.g. `tsc -p tsconfig.app.json && tsc -p tsconfig.node.json`.",
  "db:migrate": "npx prisma migrate dev",
  "db:studio": "npx prisma studio",
  "checkpoint": "npx tsc --noEmit && npm run lint && npm test -- --run && npm run build",
  "filesize": "node scripts/check-file-sizes.mjs"
}
