{
  "_preset": "desktop",
  "dev": "npm run tauri:dev",
  "build": "npm run tauri:build",
  "test": "npm test",
  "test:rust": "cd src-tauri && cargo test",
  "lint": "npm run lint && cd src-tauri && cargo clippy -- -D warnings",
  "typecheck": "npx tsc --noEmit",
  "checkpoint": "npx tsc --noEmit && npm test -- --run && cd src-tauri && cargo test && cargo clippy -- -D warnings",
  "filesize": "node scripts/check-file-sizes.mjs"
}
