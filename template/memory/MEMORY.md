# Memory Index

One line per lesson. Detail lives in the linked file. Keep entries short — this
index is read at session start, so it must stay skimmable.

Format:

```
- [Short title](file-name.md) — the one-line hook that tells you whether to open it.
```

A note with no line here is a note nobody finds. See `.claude/rules/memory.md`
for what is worth capturing and what is not.

## Lessons

_(empty — add the first one the next time a bug takes real digging)_
