# NOTE: settings.local.json is NOT gitignored — this repo IS the template.
# The template/ folder ships settings.json as the distributed config;
# the root .claude/settings.local.json is the template project's own config.

# Personal working notes (not part of the template)
NEXT_SESSION.md
Plan.md

# Dependencies
node_modules/

# Build / tool output
dist/
build/
.next/
.vite/
graphify-out/
.graphify_*

# E2E artifacts
e2e/.auth/
playwright-report/
test-results/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
*.swp
